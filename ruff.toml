target-version = "py313"
line-length = 100

[lint]
# Keep the public baseline deliberately correctness-focused. FastAPI's
# dependency-injection defaults are valid framework usage, while broader style
# and modernization rules can be adopted incrementally without hiding defects.
select = ["E4", "E7", "E9", "F"]
